package com.contracts.contracts.repository;


import com.contracts.contracts.models.DisProtocol;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DisProtocolRepo extends JpaRepository<DisProtocol, Long> {
}
